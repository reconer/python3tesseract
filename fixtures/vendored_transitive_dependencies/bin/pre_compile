#!/bin/bash

# uncomment to configure easy_install to source dependencies from ../vendor instead of pypi

echo "[easy_install]
allow_hosts = ''
find_links = $( cd "$(dirname "${BASH_SOURCE[0]}")" ; pwd -P )/../vendor/
" > ~/.pydistutils.cfg

cat ~/.pydistutils.cfg
